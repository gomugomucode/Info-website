import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import { ThemeProvider } from "@/components/theme-provider";
import { Navbar } from "@/components/layout/navbar";
import { Footer } from "@/components/layout/footer";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: {
    default: "Anupam Baral — Developer Knowledge Hub",
    template: "%s | Anupam Baral",
  },
  description: "Tutorials, AI experiments, developer notes, Next.js guides, FastAPI resources, and real-world full-stack engineering insights.",
  openGraph: {
    title: "Anupam Baral — Developer Knowledge Hub",
    description: "Tutorials, AI experiments, developer notes, Next.js guides, FastAPI resources, and real-world full-stack engineering insights.",
    url: "https://anupambaral.info.np",
    siteName: "Anupam Baral Info",
    locale: "en_US",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={`${geistSans.variable} ${geistMono.variable} min-h-screen bg-background font-sans text-foreground antialiased`}>
        <ThemeProvider attribute="class" defaultTheme="system" enableSystem disableTransitionOnChange>
          <div className="relative flex min-h-screen flex-col">
            <Navbar />
            <main className="flex-1">{children}</main>
            <Footer />
          </div>
        </ThemeProvider>
      </body>
    </html>
  );
}
